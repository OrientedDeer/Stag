{
  "name": "stag-backend",
  "version": "0.1.0",
  "private": true,
  "description": "Zero-knowledge per-user encrypted-blob store for Stag self-hosting (the /backup contract).",
  "scripts": {
    "build": "tsc",
    "start": "node dist/server.js",
    "dev": "tsc -w",
    "test": "tsc -p tsconfig.test.json && node --test dist/server.test.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "google-auth-library": "^10.9.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/node": "^20.17.0",
    "typescript": "^5.7.3"
  }
}
